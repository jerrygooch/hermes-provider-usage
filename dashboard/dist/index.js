(() => {
  const root = document.getElementById('root') || document.body
  root.className = 'provider-usage-dashboard-note'
  root.textContent = 'Provider Usage is a Hermes Desktop plugin. Install the Desktop half to use the status chip and provider pane.'
})()
